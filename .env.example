# Single key
BBD_API_KEY=your_backboard_api_key_here

# Multiple keys (comma-separated) — round-robin load balancing + auto failover
# BBD_API_KEY=key1,key2,key3

# Optional (defaults shown)
BBD_UPSTREAM=https://app.backboard.io/api
HOST=0.0.0.0
PORT=10088
THREAD_TTL=1800
DEBUG=1
